---

# dynamic_inventory also gathers cluster_hosts_state. all schemes need cluster_hosts_state; inventory is needed only for _scheme_rmvm_keepdisk_rollback, but the dynamic_inventory overhead is minimal.
dependencies:
  - role: dynamic_inventory
